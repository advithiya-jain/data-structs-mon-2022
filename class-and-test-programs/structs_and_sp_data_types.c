#include<stdio.h>
#include<stdlib.h>

int main(int argc, char **argv) {
    int *a;
    int i, n val;

    // Enter Data
    scanf("%d", &val);
    while(val > 0) {
        // do stuff
        
        scanf("%d", &val)
    }
    // Print Data
    for(i = 0; i < n; i++) {
        printf("%d", a[i]);
    }

    printf("\n\nDone!\n");

    return 0;
}